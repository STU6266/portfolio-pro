<%- include("partials/head") %>
<%- include("partials/header") %>
<%- include("partials/nav") %>

<main class="resume-layout">
  <!--
    Main Hangman container.

    We wrap the whole game in a single card so that it visually
    matches your other portfolio sections (Projects, Filament Finder, etc.).
    The "hangman-card" class is styled in style.css to control width, spacing, etc.
  -->
  <section class="card hangman-card">
    <!--
      Header: short explanation what this project is.

      The "project-tag" and "project-description" classes re-use your
      existing styling so this card looks consistent with your project cards.
    -->
    <header class="hangman-header">
      <p class="project-tag">GAME · C# → JavaScript</p>
      <h1>Hangman (Web version)</h1>
      <p class="project-description">
        This is a small browser version of my original C# console Hangman game.
        You can pick a difficulty, guess letters, and see the ASCII hangman
        being drawn step by step.
      </p>
    </header>

    <!--
      Controls section: difficulty selector + "Start new game" button.

      Layout hint:
      - The .hangman-controls container uses flexbox so that the dropdown
        and button sit nicely next to each other (or wrap on small screens).
    -->
    <section class="hangman-controls">
      <!-- Difficulty select -->
      <div class="hangman-field">
        <label for="hangman-difficulty">Difficulty</label>
        <select id="hangman-difficulty">
          <option value="easy">Easy (short words)</option>
          <option value="medium">Medium</option>
          <option value="hard">Hard (long words)</option>
        </select>
      </div>

      <!-- Start new game button -->
      <button id="hangman-new-game" class="btn-primary">
        Start new game
      </button>
    </section>

    <!--
      Status section: shows the current word, counters and feedback text.

      The layout uses a two-column grid:
      - Left: the big word display (underscores + letters)
      - Right: info such as wrong guesses, guessed letters and the message
    -->
    <section class="hangman-status">
      <!-- Left side: the word itself -->
      <div class="hangman-word-wrapper">
        <h2>Word</h2>
        <p id="hangman-word" class="hangman-word">
          <!-- underscores and letters are injected here by hangman.js -->
        </p>
      </div>

      <!-- Right side: counters + feedback -->
      <div class="hangman-info">
        <p>
          Wrong guesses:
          <span id="hangman-wrong-count">0</span> / 
          <span id="hangman-max">10</span>
        </p>
        <p>
          Guessed letters:
          <span id="hangman-guessed-letters">–</span>
        </p>
        <p id="hangman-message" class="hangman-message">
          Click "Start new game" to begin.
        </p>
      </div>
    </section>

    <!--
      ASCII hangman figure (similar idea as your C# PrintHangmanFigure).

      The <pre> tag keeps the spacing and line breaks exactly as defined
      in the template strings inside hangman.js.
    -->
    <section class="hangman-figure-section">
      <h2>Hangman</h2>
      <pre id="hangman-figure" class="hangman-figure">
<!-- figure will be drawn here -->
      </pre>
    </section>

    <!--
      On-screen keyboard for A–Z.

      The buttons are not written by hand in HTML. Instead, hangman.js
      builds 26 buttons (A–Z) dynamically so the code stays short and
      easy to change later.
    -->
    <section class="hangman-keyboard-section">
      <h2>Guess a letter</h2>
      <div id="hangman-keyboard" class="hangman-keyboard">
        <!-- buttons A–Z will be created by JS -->
      </div>
    </section>
  </section>

  <!--
    Attach the Hangman JS logic.

    The "defer" attribute makes sure the script runs after the HTML
    has been parsed, which fits well with our DOMContentLoaded handler
    inside public/js/hangman.js.
  -->
  <script src="/js/hangman.js" defer></script>
</main>

<%- include("partials/footer") %>

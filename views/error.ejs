<%- include("partials/head") %>
<%- include("partials/header") %>
<%- include("partials/nav") %>

<!--
  error.ejs
  ---------------------------------------------------------------------------
  Generic error template used by both:
  - the 404 "not found" handler, and
  - the generic server error handler.
  The controller / middleware passes "title" and "message" into the view.
-->

<main class="resume-layout">
  <section class="card about-card">
    <h1><%= title %></h1>
    <p><%= message %></p>
  </section>
</main>

<%- include("partials/footer") %>
